# Configurazione per Flesk AI Overview
# Copia questo file in .env e inserisci i tuoi valori reali

# API Key per Google Gemini (OBBLIGATORIA)
# Ottieni la tua API key da: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Configurazione Flask
FLASK_ENV=production
FLASK_DEBUG=False

# Configurazione per il deployment
PORT=5000

# Note per il deployment su Render:
# 1. Aggiungi GEMINI_API_KEY come variabile d'ambiente nel dashboard di Render
# 2. Non committare mai il file .env con le chiavi reali
# 3. L'applicazione è configurata per funzionare con gunicorn in produzione